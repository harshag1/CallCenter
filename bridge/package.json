{
  "name": "harshas-amazing-call-center-bridge",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": { "node": "^20.19.0 || ^22.13.0 || >=24.0.0" },
  "description": "Twilio Media Streams <-> provider-neutral realtime audio bridge",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && for file in lib/*.js; do node --check \"$file\"; done",
    "test": "node --test test/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
