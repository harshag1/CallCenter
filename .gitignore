CLAUDE.md

# Secrets — never commit
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Dependencies / builds
node_modules/
.next/
.vercel/
*.tsbuildinfo

# Python environments / caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store

# Local benchmark evidence can contain large audio, raw provider payloads, and
# resumable partial journals. Protocols, scenarios, and curated redacted reports
# remain intentionally trackable.
benchmarks/voice-long-horizon/results/
benchmarks/voice-long-horizon/.local/
benchmarks/voice-long-horizon/fixtures/generated/
